import {parseData} from "./day14";

const testData = ``.split('\n');


test('It parses', ()=>{
    const data = parseData(testData)
})

test('part one works', ()=>{

})

test('part two works', ()=>{

})